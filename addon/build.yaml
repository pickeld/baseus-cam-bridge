build_from:
  aarch64: ghcr.io/home-assistant/aarch64-base-debian:bookworm
  amd64: ghcr.io/home-assistant/amd64-base-debian:bookworm
  armv7: ghcr.io/home-assistant/armv7-base-debian:bookworm
labels:
  org.opencontainers.image.title: Baseus Cam Bridge
  org.opencontainers.image.source: https://github.com/pickeld/baseus-cam-bridge
args:
  MEDIAMTX_VERSION: "1.11.3"
  # Pin to an exact commit so each release busts Docker's layer cache and
  # always installs the intended bridge code (bump this with the version).
  BASEUS_REF: "6e2681f70ec8214648a5a23fbb468b21b0d77a2e"
